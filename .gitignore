# Data & model artifacts (generated, not source - models are written to
# MinIO/S3A by the pipeline, never to a local path, so there's no local
# models dir to exclude here)
data/raw/
data/processed/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.venv-ingest/
.venv-spark/
.ipynb_checkpoints/
.pytest_cache/

# Spark / Hadoop
spark-warehouse/
metastore_db/
derby.log
*.crc
_SUCCESS

# Env / secrets
.env

# OS / editor
.DS_Store
.vscode/
.idea/

# Docker
*.log

# Application logs (mounted volume, see docker-compose.yml)
logs/*
!logs/.gitkeep
